from django.contrib.auth.models import AbstractUser
from django.db import models

class CustomUser(AbstractUser):     # inherits the AbstractUser
    pass    # serves as the placeholder to not make any changes yet

